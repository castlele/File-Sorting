TODO:
1. Add error handling:
	a) Error enumeration + rawValue;
	b) Better Info about errors.
	
2. Add better user interface:
	a) use prints while user uses the program;
	b) add --help to SortingWay 

3. Add main func to main.swift:
	a) add user interface;
	
